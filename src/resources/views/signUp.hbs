<div class="container">
    <div class="row">
        <div class="col"></div>
        <div class="col-6 bg-light mt-5">

            <form action="" method="POST" class="form col-11" id="form-1">
                <h3 class="heading text-center">Sign Up</h3>

                <div class="spacer"></div>

                <div class="form-group">
                    <label for="fullname" class="form-label">Full Name</label>
                    <input id="fullname" name="fullname" type="text" placeholder="VD: lee hoang" class="form-control"
                     value="{{user.fullname}}" required>
                </div>

                <div class="form-group">
                    <label for="email" class="form-label">Email</label>
                    <input id="email" name="email" type="text" placeholder="VD: email@domain.com" class="form-control"
                     value="{{user.email}}" required>
                </div>

                <div class="form-group">
                    <label for="password" class="form-label">Password</label>
                    <input id="password" name="password" type="password" placeholder="input password" class="form-control"
                    value="{{user.password}}" required> 
                </div>

                <div class="form-group">
                    <label for="password_confirmation" class="form-label">Confirm Password</label>
                    <input id="password_confirmation" name="password_confirmation" placeholder="input password again"
                        type="password" class="form-control" value="{{user.password_confirmation}}" required>
                </div>

                <button class="form-submit btn btn-primary">Sign Up</button>
            </form>
        </div>
        <div class="col">
            
            {{#if errors}}
                {{#each errors}}
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                        <strong>Error</strong> {{this.msg}}
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                {{/each}}
            {{/if}}
        </div>
    </div>
</div>


<script>

    Validator({

        form: 'form-1',
        rules: [
            Validator.isRequired('#fullname'),
            Validator.isEmail('#email'),
        ]
    });

</script>